{
  "name": "capacitor-background-geolocation-example",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Capacitor 8 host app exercising @gachlab/capacitor-background-geolocation.",
  "scripts": {
    "copy:plugin": "node scripts/copy-plugin.mjs",
    "sync": "npm run copy:plugin && cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@gachlab/capacitor-background-geolocation": "file:.."
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "esbuild": "^0.28.0"
  }
}
