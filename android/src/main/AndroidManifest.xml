<?xml version="1.0" encoding="utf-8"?>
<!--
    SPDX-License-Identifier: MIT
    Copyright (c) 2026 gachlab
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.location" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <!-- Foreground location service — pure Kotlin, MIT-licensed. -->
        <service
            android:name="com.gachlab.geolocation.service.LocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- WorkManager background sync (replaces the old SyncAdapter/AuthenticatorService). -->
        <!-- WorkManager registers its own services via manifest merger — no entry needed here. -->

        <!-- DistanceFilterLocationProvider alarm/intent receiver targets (must be in manifest). -->
        <receiver
            android:name="com.gachlab.geolocation.provider.DistanceFilterLocationProvider$StationaryAlarmReceiver"
            android:enabled="true"
            android:exported="false" />
        <receiver
            android:name="com.gachlab.geolocation.provider.DistanceFilterLocationProvider$StationaryMonitorReceiver"
            android:enabled="true"
            android:exported="false" />
        <!-- ActivityLocationProvider activity recognition intent target. -->
        <receiver
            android:name="com.gachlab.geolocation.provider.ActivityLocationProvider$ActivityUpdateReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Receives geofence transition intents from Google Play Services. -->
        <receiver
            android:name="com.gachlab.geolocation.GeofenceBroadcastReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Restart the service after device reboot when startOnBoot is enabled. -->
        <receiver
            android:name="com.gachlab.geolocation.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
